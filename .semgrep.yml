# Semgrep configuration for smart contract security scanning
# Semgrep uses 'auto' config by default which includes security rules
# This file can be extended with custom rules if needed
# Usage: semgrep --config=auto contracts/
